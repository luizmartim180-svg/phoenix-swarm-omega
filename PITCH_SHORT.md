Phoenix Swarm (Omega) — Short pitch

Omega runs the multi-agent analysis loop that evaluates and cross-checks remediation options using both local knowledge and embeddings. For the demo: run two offline cycles, present the final insights_offline.md, and show how the system degrades gracefully when external services are unavailable.
